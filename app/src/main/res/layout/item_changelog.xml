<?xml version="1.0" encoding="utf-8"?>
<!-- 不用 MaterialCardView：本 App 基主题非 Theme.MaterialComponents，
     MaterialCardView 构造时会抛 "requires Theme.MaterialComponents" 导致 inflate 崩溃。
     卡片背景改由 ChangelogAdapter 用 CardGroupDecoration.shapeFor 设置（card_bg + 14dp 圆角），
     与设置页卡片同源。 -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginVertical="6dp"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:id="@+id/title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="@style/TextAppearance.Settings.Title"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/items"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:lineSpacingExtra="4dp"
        android:textColor="?android:attr/textColorPrimary"
        android:textSize="14sp" />
</LinearLayout>
